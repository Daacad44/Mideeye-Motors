/**
 * Official brand constants. The logo image itself is NEVER hardcoded here —
 * it's uploaded once via the Admin Media Library (ImageKit) and
 * served through `/api/branding`, which resolves the current logo's
 * delivery URL server-side. See `lib/branding.ts`.
 */
export const BRAND_NAME = 'MIDEEYE MOTORS';
export const BRAND_SUB = '& Rental Car Co.';
